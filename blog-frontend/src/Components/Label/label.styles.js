import styled from "styled-components";
import MainTheme from "../../Theme";
const Label = styled.label`
  color: ${MainTheme.fontColors.gray};
  font-family: ${MainTheme.fontFamily.roboto};
  font-style: ${MainTheme.fontSize.medium};
`;

export { Label };
