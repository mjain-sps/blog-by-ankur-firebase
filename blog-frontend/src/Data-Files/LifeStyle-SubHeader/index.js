import { lifeStylesAllData } from "./lifestyles.all";
import { lifeStylesFashionData } from "./lifestyle.fashion";

const lifeStylesData = {
  lifeStylesAllData,
  lifeStylesFashionData,
};

export default lifeStylesData;
